---
// Solo envuelve la lista y pone el título.
// El contenido de los semestres viene por <slot />.
const { titulo = 'Plan de estudios por semestre' } = Astro.props;
---

<section class="cies-semestres-timeline my-5">
  <h2 class="h5 fw-bold mb-4">{titulo}</h2>

  <ol class="cies-timeline list-unstyled m-0">
    <slot />
  </ol>
</section>
