---
const { kicker, titulo, descripcion } = Astro.props;
---

<header class="text-center mb-4">
  <p class="cies-section-kicker mb-1">{kicker}</p>
  <h2 class="cies-section-title mb-2">{titulo}</h2>
  <p class="text-muted mb-0">{descripcion}</p>
</header>
